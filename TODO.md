## Task: Remove expiring date from uploaded PDFs/UI

- [ ] Inspect relevant code paths (done): server.js sets expiresAt null; UI uses f.expiresAt to render expiry.
- [ ] Edit `public/index.html` to remove expiry display from file cards.
- [x] Edit `public/index.html` to remove expiry row from preview modal and remove related JS UI calculations.

- [ ] Verify by running/reloading the app.

